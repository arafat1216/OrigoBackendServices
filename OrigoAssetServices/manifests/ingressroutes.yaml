apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: origoassets-ingressroute
  namespace: develop
spec:
  routes:
  - match: Host(`origov2dev.mytos.no`) && PathPrefix(`/origoapi/assets`)
    kind: Rule
    services:
    - name: origoassetservices
      port: https
  tls:
    certResolver: letsencrypt